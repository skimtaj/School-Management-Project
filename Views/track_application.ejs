<!DOCTYPE html>
<html lang="en">

<%-include('home_heaer')%>

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Scholarship Application | Pragati Scholarship</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
            rel="stylesheet">

        <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"
            integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n"
            crossorigin="anonymous"></script>

        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css"
            integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <style>
            :root {
                --primary-green: #059669;
                --primary-green-dark: #047857;
                --primary-green-light: #10b981;
                --accent-gold: #d97706;
                --light-bg: #f0fdf4;
                --pure-white: #ffffff;
                --dark-text: #1f2937;
                --gray-text: #6b7280;
                --light-gray: #e5e7eb;
                --border-gray: #d1d5db;
                --error-red: #dc2626;
                --success-green: #059669;
                --focus-ring: rgba(5, 150, 105, 0.3);
            }

            * {
                box-sizing: border-box;
                margin: 0;
                padding: 0;
            }

            body {
                font-family: 'Inter', sans-serif;
                background-color: var(--light-bg);
                color: var(--dark-text);
                line-height: 1.5;
                -webkit-font-smoothing: antialiased;
                min-height: 100vh;
                display: flex;
                flex-direction: column;
            }

            .main-content {
                flex: 1;
                display: flex;
                flex-direction: column;
            }

            .scholarship-container {
                display: flex;
                justify-content: center;
                align-items: center;
                flex: 1;
                padding: 2rem 1rem;
                margin-top: 80px;
                box-sizing: border-box;
            }

            .scholarship-card {
                background: var(--pure-white);
                border-radius: 12px;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05),
                    0 2px 4px -1px rgba(0, 0, 0, 0.02);
                border: 1px solid var(--border-gray);
                width: 100%;
                max-width: 520px;
                overflow: hidden;
            }

            .scholarship-header {
                background: linear-gradient(135deg, var(--primary-green), var(--primary-green-dark));
                color: var(--pure-white);
                padding: 2.5rem 2rem;
                text-align: center;
                position: relative;
            }

            .scholarship-logo {
                width: 72px;
                height: 72px;
                background-color: var(--pure-white);
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                margin: 0 auto 1.5rem;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            }

            .scholarship-logo i {
                color: var(--primary-green);
                font-size: 2rem;
            }

            .scholarship-title {
                font-size: 1.5rem;
                font-weight: 600;
                margin-bottom: 0.5rem;
                letter-spacing: -0.025em;
            }

            .scholarship-subtitle {
                opacity: 0.9;
                font-weight: 400;
                font-size: 0.875rem;
                max-width: 80%;
                margin: 0 auto;
                line-height: 1.5;
            }

            .scholarship-body {
                padding: 2.5rem 2rem;
            }

            .form-group {
                margin-bottom: 1.5rem;
                position: relative;
            }

            .form-label {
                display: block;
                margin-bottom: 0.5rem;
                font-weight: 500;
                color: var(--dark-text);
                font-size: 0.875rem;
                letter-spacing: 0.025em;
            }

            .form-label.required::after {
                content: " *";
                color: var(--error-red);
            }

            .form-input {
                width: 100%;
                padding: 0.65rem 1.25rem;
                border: 1px solid var(--border-gray);
                border-radius: 8px;
                font-size: 0.9375rem;
                transition: all 0.2s ease;
                background-color: var(--pure-white);
                color: var(--dark-text);
                line-height: 1.5;
                height: 42px;
            }

            .form-input:focus {
                outline: none;
                border-color: var(--primary-green);
                box-shadow: 0 0 0 3px var(--focus-ring);
            }

            .form-input::placeholder {
                color: var(--gray-text);
                opacity: 0.7;
                font-weight: 400;
            }

            input[type="date"] {
                appearance: none;
                background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%236b7280' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpath d='M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z'%3e%3c/path%3e%3cpolyline points='17 21 17 13 7 13 7 21'%3e%3c/polyline%3e%3cpolyline points='7 3 7 8 15 8'%3e%3c/polyline%3e%3c/svg%3e");
                background-repeat: no-repeat;
                background-position: right 1rem center;
                background-size: 1.125rem;
            }

            .submit-btn {
                width: 100%;
                padding: 0.75rem;
                border: none;
                border-radius: 8px;
                font-size: 0.9375rem;
                font-weight: 500;
                cursor: pointer;
                transition: all 0.2s ease;
                background: linear-gradient(135deg, var(--primary-green), var(--primary-green-dark));
                color: var(--pure-white);
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 0.75rem;
                letter-spacing: 0.025em;
                height: 42px;
            }

            .submit-btn:hover {
                background: linear-gradient(135deg, var(--primary-green-dark), var(--primary-green));
            }

            .submit-btn:focus {
                outline: none;
                box-shadow: 0 0 0 3px var(--focus-ring);
            }

            .form-footer {
                margin-top: 1.5rem;
                text-align: center;
                font-size: 0.8125rem;
                color: var(--gray-text);
            }

            .form-footer a {
                color: var(--primary-green);
                text-decoration: none;
                font-weight: 500;
            }

            .form-footer a:hover {
                text-decoration: underline;
            }

            @media (max-width: 768px) {
                .scholarship-container {
                    padding: 1.5rem 1rem;
                    margin-top: 70px;
                }

                .scholarship-header {
                    padding: 2rem 1.5rem;
                }

                .scholarship-body {
                    padding: 2rem 1.5rem;
                }

                .scholarship-logo {
                    width: 64px;
                    height: 64px;
                }

                .scholarship-title {
                    font-size: 1.375rem;
                }
            }

            @media (max-width: 480px) {
                .scholarship-header {
                    padding: 1.75rem 1.25rem;
                }

                .scholarship-body {
                    padding: 1.75rem 1.25rem;
                }

                .scholarship-logo {
                    width: 56px;
                    height: 56px;
                }

                .scholarship-title {
                    font-size: 1.25rem;
                }

                .form-input {
                    padding: 0.6rem 1rem;
                    height: 40px;
                }

                .submit-btn {
                    padding: 0.7rem;
                    height: 40px;
                }
            }
        </style>
    </head>

    <body>
        <div class="main-content">
            <div class="scholarship-container">
                <div class="scholarship-card">
                    <div class="scholarship-header">
                        <div class="scholarship-logo">
                            <i class="fas fa-graduation-cap"></i>
                        </div>
                        <h1 class="scholarship-title">Scholarship Application</h1>
                        <p class="scholarship-subtitle">Please verify your identity to access your scholarship
                            application</p>
                    </div>


                    <% if(messages.success) {%>

                        <div class="alert alert-success" id="msg">
                            <%= messages.success %>
                        </div>

                        <%} %>

                            <% if(messages.error){%>

                                <div class="alert alert-danger" id="msg">
                                    <%= messages.error %>
                                </div>

                                <%} %>



                                    <!-- Form Body -->
                                    <div class="scholarship-body">
                                        <form id="scholarshipForm" method="POST">
                                            <!-- Application ID Field -->
                                            <div class="form-group">
                                                <label for="applicationId" class="form-label required">Application
                                                    ID</label>
                                                <input type="text" id="applicationId" name="applicationId"
                                                    class="form-input" required placeholder="e.g. SCH-2023-00145"
                                                    pattern="[A-Za-z0-9-]+" minlength="8"
                                                    title="Please enter your application ID in the correct format">
                                            </div>

                                            <div class="form-group">
                                                <label for="dob" class="form-label required">Date of Birth</label>
                                                <input type="date" id="dob" name="dob" class="form-input" required
                                                    max="<%= new Date().toISOString().split('T')[0] %>">
                                            </div>

                                            <div class="form-group">
                                                <label for="mobile" class="form-label required">Mobile Number</label>
                                                <input type="tel" id="mobile" name="mobile" class="form-input" required
                                                    placeholder="+1 (123) 456-7890"
                                                    title="Please enter a valid international phone number starting with +">
                                            </div>

                                            <div class="form-group">
                                                <button type="submit" class="submit-btn">
                                                    <i class="fas fa-paper-plane"></i> Submit Application
                                                </button>
                                            </div>
                                        </form>

                                        <div class="form-footer">
                                            Need help? <a href="#">Contact our support team</a>
                                        </div>
                                    </div>
                </div>
            </div>
        </div>

        <%-include('footer')%>


    </body>

    <script>

        setTimeout(() => {

            document.getElementById('msg').style.display = 'none'

        }, 4000)

    </script>

</html>