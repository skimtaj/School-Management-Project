<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Course | EduManage</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #2a9d8f;
            --primary-dark: #21867a;
            --secondary-color: #f8f9fa;
            --text-color: #264653;
            --border-color: #ced4da;
            --error-color: #e76f51;
            --success-color: #2a9d8f;
            --light-bg: #f1faee;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-bg);
            color: var(--text-color);
            line-height: 1.6;
            padding-top: var(--header-height);
            min-height: 100vh;
        }

        .main-content {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 30px 20px;
            min-height: calc(100vh - var(--header-height));
        }

        .container {
            width: 100%;
            max-width: 600px;
            padding: 40px;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(42, 157, 143, 0.15);
            position: relative;
            overflow: hidden;
        }

        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, var(--primary-color), #a8dadc);
        }

        .logo-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .logo {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background-color: var(--primary-color);
            color: white;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            font-size: 28px;
            margin-bottom: 15px;
            box-shadow: 0 4px 12px rgba(42, 157, 143, 0.3);
        }

        h1 {
            text-align: center;
            margin-bottom: 10px;
            color: black;
            font-weight: 600;
            font-size: 28px;
        }

        .subtitle {
            text-align: center;
            color: #6c757d;
            margin-bottom: 30px;
            font-weight: 400;
            font-size: 16px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #495057;
            font-size: 15px;
        }

        .form-control {
            width: 100%;
            padding: 14px 18px;
            font-size: 16px;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            transition: all 0.3s;
            background-color: #f8f9fa;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(42, 157, 143, 0.2);
            outline: none;
            background-color: white;
        }

        .input-group {
            position: relative;
            display: flex;
            align-items: center;
        }

        .input-group-prepend {
            position: absolute;
            left: 15px;
            color: #6c757d;
            font-weight: 500;
            z-index: 1;
        }

        .input-group input {
            padding-left: 45px;
        }

        .btn {
            display: inline-block;
            padding: 14px 24px;
            font-size: 16px;
            font-weight: 600;
            text-align: center;
            white-space: nowrap;
            vertical-align: middle;
            cursor: pointer;
            border: none;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: white;
            width: 100%;
            margin-top: 10px;
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(42, 157, 143, 0.3);
        }

        .btn-primary:active {
            transform: translateY(0);
        }

        .validation-message {
            font-size: 14px;
            margin-top: 8px;
            display: none;
            padding-left: 5px;
        }

        .error {
            color: var(--error-color);
        }

        .success {
            color: var(--success-color);
        }

        .footer-text {
            text-align: center;
            margin-top: 25px;
            color: #6c757d;
            font-size: 14px;
        }

        @media (max-width: 768px) {
            .container {
                padding: 30px;
            }

            h1 {
                font-size: 24px;
            }
        }

        @media (max-width: 480px) {
            .main-content {
                padding: 15px;
            }

            .container {
                padding: 25px 20px;
            }

            .logo {
                width: 50px;
                height: 50px;
                font-size: 24px;
            }

            .form-control {
                padding: 12px 15px;
            }

            .btn {
                padding: 12px 20px;
            }
        }
    </style>
</head>

<body>
    <%-include('admin_header')%>

        <div class="main-content">
            <div class="container">
                <div class="logo-container">
                    <div class="logo">
                        <i class="fas fa-graduation-cap"></i>
                    </div>
                    <h1>Add New Academic</h1>
                    <p class="subtitle">Expand your educational offerings with new academic</p>
                </div>

                <form id="courseForm" method="post">
                    <div class="form-group">
                        <label for="courseName">Academic Year </label>
                        <input type="text" class="form-control" id="courseName" placeholder="2020-2021" required
                            name="academic_year">
                        <div class="validation-message error" id="nameError">Course name must be at least 3 characters
                        </div>
                    </div>



                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-plus-circle" style="margin-right: 8px;"></i> Add Academic Year
                    </button>

                    <p class="footer-text">All fields are required. Courses will be reviewed before publishing.</p>
                </form>
            </div>
        </div>


</body>

</html>