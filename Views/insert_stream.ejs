<!DOCTYPE html>
<html lang="en">


    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Add New Stream | EduManage</title>
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
            rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <style>
            :root {
                --primary-color: #2C3E50;
                --accent-color: #00A65A;
                --accent-dark: #00894A;
                --border-color: #BDC3C7;
                --text-color: #2C3E50;
                --light-bg: #F4F6F7;
            }

            * {
                box-sizing: border-box;
                margin: 0;
                padding: 0;
            }

            body {
                font-family: 'Poppins', sans-serif;
                background-color: var(--light-bg);
                color: var(--text-color);
            }

            .main-content {
                display: flex;
                justify-content: center;
                align-items: center;
                padding: 30px 20px;
                min-height: 100vh;
            }

            .container {
                width: 100%;
                max-width: 500px;
                padding: 40px;
                background-color: white;
                border-radius: 12px;
                box-shadow: 0 8px 25px rgba(44, 62, 80, 0.15);
                position: relative;
            }

            .container::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 6px;
                background: linear-gradient(90deg, var(--accent-color), var(--primary-color));
            }

            .logo-container {
                text-align: center;
                margin-bottom: 20px;
            }

            .logo {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                background-color: var(--accent-color);
                color: white;
                width: 60px;
                height: 60px;
                border-radius: 50%;
                font-size: 28px;
                margin-bottom: 15px;
                box-shadow: 0 4px 12px rgba(0, 166, 90, 0.3);
            }

            h1 {
                text-align: center;
                margin-bottom: 10px;
                color: var(--primary-color);
                font-weight: 600;
                font-size: 26px;
            }

            .subtitle {
                text-align: center;
                color: #7f8c8d;
                margin-bottom: 30px;
                font-weight: 400;
                font-size: 15px;
            }

            /* Floating Label Input */
            .form-group {
                position: relative;
                margin-bottom: 25px;
            }

            .form-control {
                width: 100%;
                padding: 14px 14px;
                font-size: 16px;
                border: 1.5px solid var(--border-color);
                border-radius: 8px;
                background-color: transparent;
                outline: none;
                transition: 0.3s;
            }

            .form-control:focus {
                border-color: var(--accent-color);
                box-shadow: 0 0 0 3px rgba(0, 166, 90, 0.15);
            }

            .form-group label {
                position: absolute;
                top: 50%;
                left: 14px;
                transform: translateY(-50%);
                background-color: white;
                padding: 0 6px;
                color: #7f8c8d;
                font-size: 15px;
                pointer-events: none;
                transition: 0.3s ease all;
            }

            .form-control:focus+label,
            .form-control:not(:placeholder-shown)+label {
                top: -8px;
                left: 10px;
                font-size: 12px;
                color: var(--accent-color);
            }

            .btn {
                display: inline-block;
                padding: 14px 24px;
                font-size: 16px;
                font-weight: 600;
                text-align: center;
                cursor: pointer;
                border: none;
                border-radius: 8px;
                transition: all 0.3s;
                width: 100%;
            }

            .btn-primary {
                background-color: var(--accent-color);
                color: white;
            }

            .btn-primary:hover {
                background-color: var(--accent-dark);
                transform: translateY(-2px);
                box-shadow: 0 6px 15px rgba(0, 166, 90, 0.3);
            }

            .footer-text {
                text-align: center;
                margin-top: 20px;
                color: #7f8c8d;
                font-size: 13px;
            }

            @media (max-width: 480px) {
                .container {
                    padding: 25px 20px;
                }

                .logo {
                    width: 50px;
                    height: 50px;
                    font-size: 24px;
                }

                .btn {
                    padding: 12px 20px;
                }
            }
        </style>
    </head>

    <body>

        <div class="main-content">
            <div class="container">
                <div class="logo-container">
                    <div class="logo">
                        <i class="fas fa-stream"></i>
                    </div>
                    <h1>Add New Stream</h1>
                    <p class="subtitle">Organize your courses better with streams</p>
                </div>

                <form method="post">
                    <div class="form-group">
                        <input type="text" class="form-control" id="streamName" placeholder=" " required
                            name="Stream_Name">
                        <label for="streamName">Stream Name</label>
                    </div>

                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-plus-circle" style="margin-right: 8px;"></i> Add Stream
                    </button>

                    <p class="footer-text">All fields are required. Streams will be reviewed before publishing.</p>
                </form>
            </div>
        </div>

    </body>

</html>