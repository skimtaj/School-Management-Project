<!DOCTYPE html>
<html lang="en">
<%-include('home_heaer')%>

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Scholarship Portal</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

        <style>
            :root {
                --primary: #3a7bd5;
                --secondary: #00d2ff;
                --accent: #ff5e62;
                --light: #f8f9fa;
                --dark: #212529;
                --text: #495057;
            }

            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
                font-family: 'Poppins', sans-serif;
            }

            body {
                background: linear-gradient(135deg, #f5f7fa 0%, #e4e8f0 100%);
                color: var(--text);
                min-height: 100vh;
                display: flex;
                flex-direction: column;
            }

            @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');

            main {
                flex: 1;
                padding: 2rem 0;
            }

            .container {
                max-width: 1200px;
                margin: 0 auto;
                padding: 0 2rem;
                margin-top: 30px;
            }

            .page-header {
                text-align: center;
                margin-bottom: 3rem;
            }

            .page-title {
                font-size: 2.5rem;
                margin-bottom: 1rem;
                color: var(--dark);
                position: relative;
                display: inline-block;
            }

            .page-title:after {
                content: '';
                position: absolute;
                bottom: -10px;
                left: 50%;
                transform: translateX(-50%);
                width: 80px;
                height: 4px;
                background: linear-gradient(to right, var(--primary), var(--secondary));
                border-radius: 2px;
            }

            .page-subtitle {
                color: var(--text);
                font-weight: 300;
                max-width: 700px;
                margin: 0 auto;
                margin-top: 10px;
            }

            .content-grid {
                display: grid;
                grid-template-columns: 2fr 1fr;
                gap: 2rem;
            }

            .rules-card {
                background: white;
                border-radius: 12px;
                box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
                padding: 2.5rem;
                margin-bottom: 2rem;
            }

            .section-title {
                font-size: 1.8rem;
                margin-bottom: 1.5rem;
                color: var(--dark);
                display: flex;
                align-items: center;
                gap: 1rem;
            }

            .section-title i {
                color: var(--primary);
            }

            .rules-list {
                list-style: none;
            }

            .rules-list li {
                padding: 0.8rem 0;
                border-bottom: 1px solid #eee;
                display: flex;
                align-items: flex-start;
                gap: 1rem;
            }

            .rules-list li:last-child {
                border-bottom: none;
            }

            .rule-icon {
                color: var(--secondary);
                margin-top: 3px;
            }

            .highlight {
                background-color: rgba(0, 210, 255, 0.1);
                padding: 0.2rem 0.5rem;
                border-radius: 4px;
                font-weight: 600;
                color: var(--primary);
            }

            .quick-actions {
                display: flex;
                flex-direction: column;
                gap: 1.5rem;
            }

            .action-card {
                background: white;
                border-radius: 12px;
                box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
                padding: 2rem;
                text-align: center;
                transition: all 0.3s;
            }

            .action-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 12px 40px rgba(0, 0, 0, 0.12);
            }

            .action-card.fresher {
                border-top: 4px solid var(--primary);
            }

            .action-card.registered {
                border-top: 4px solid var(--accent);
            }

            .action-title {
                font-size: 1.3rem;
                margin-bottom: 1rem;
                color: var(--dark);
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 0.8rem;
            }

            .action-btn {
                display: inline-block;
                padding: 0.8rem 1.8rem;
                border-radius: 50px;
                text-decoration: none;
                font-weight: 600;
                transition: all 0.3s;
                margin-top: 1rem;
                border: none;
                cursor: pointer;
                font-size: 1rem;
            }

            .btn-primary {
                background: linear-gradient(to right, var(--primary), var(--secondary));
                color: white;
            }

            .btn-primary:hover {
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(58, 123, 213, 0.3);
            }

            .btn-accent {
                background: linear-gradient(to right, var(--accent), #ff9966);
                color: white;
            }

            .btn-accent:hover {
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(255, 94, 98, 0.3);
            }

            @media (max-width: 992px) {
                .content-grid {
                    grid-template-columns: 1fr;
                }

                .quick-actions {
                    flex-direction: row;
                }
            }

            @media (max-width: 768px) {
                .page-title {
                    font-size: 2rem;
                }

                .quick-actions {
                    flex-direction: column;
                }
            }

            @media (max-width: 576px) {
                .container {
                    padding: 0 1rem;
                }

                .page-title {
                    font-size: 1.8rem;
                }

                .rules-card {
                    padding: 1.5rem;
                }

                .section-title {
                    font-size: 1.5rem;
                }
            }
        </style>
    </head>

    <body>
        <main>
            <div class="container">
                <div class="page-header">
                    <h1 class="page-title">2025-2026 Scholarship Program</h1>
                    <p class="page-subtitle">
                        Discover financial aid opportunities to support your academic journey.
                        Browse available scholarships and apply today.
                    </p>
                </div>

                <div class="content-grid">
                    <div class="rules-section">
                        <div class="rules-card">
                            <h2 class="section-title">
                                <i class="fas fa-file-alt"></i> Program Guidelines
                            </h2>
                            <ul class="rules-list">
                                <li>
                                    <div>
                                        Application period: <span class="highlight">October 1 - December 31,
                                            2023</span>
                                    </div>
                                </li>
                                <li>
                                    <div><b>Class Level:</b> Students must be studying between Class 5 and Class 10.
                                    </div>
                                </li>
                                <li>
                                    <div><b>Academic Performance:</b> Minimum 70% marks in the last annual
                                        examination.
                                    </div>
                                </li>
                                <li>
                                    <div><b>Family Income:</b> Annual family income below ₹1,00,000.</div>
                                </li>
                                <li>
                                    <div><b>Attendance:</b> Minimum 75% attendance in school</div>
                                </li>
                                <li>
                                    <div>Recipients must maintain satisfactory academic progress</div>
                                </li>
                            </ul>
                        </div>
                    </div>

                    <script>
                <% if (messages.success) { %>
                            Swal.fire({
                                icon: 'success',
                                title: 'Success!',
                                text: "<%= messages.success %>",
                                confirmButtonColor: '#3085d6'
                            });
                <% } %>
            
                <% if (messages.error) { %>
                            Swal.fire({
                                icon: 'error',
                                title: 'Error!',
                                text: "<%= messages.error %>",
                                confirmButtonColor: '#d33'
                            });
                <% } %>
                    </script>

                    <div class="quick-actions">
                        <div class="action-card fresher">
                            <h3 class="action-title">
                                <i class="fas fa-user-plus"></i> New Applicants
                            </h3>
                            <p>Start your scholarship application process</p>
                            <a href="/pragatischolarship/apply-scholarship" class="action-btn btn-primary">Apply
                                Now</a>
                        </div>

                        <div class="action-card registered">
                            <h3 class="action-title">
                                <i class="fas fa-user-check"></i> Track Application
                            </h3>
                            <p>Track Your Scholarship</p>
                            <a href="/pragatischolarship/track-application" class="action-btn btn-accent">Login to
                                Portal</a>
                        </div>
                    </div>
                </div>
            </div>
        </main>


        <%-include('footer')%>



    </body>

</html>